{% extends 'base.html.twig' %}
{% block title %}Hello HomeController!{% endblock %}
{% block body %}
    {% include 'components/_header.html.twig' %}
    <div class="container">
        <h1>Create new Reward</h1>
        <a class="btn btn-gold m-1 text-white" href="{{ path('admin_rewards') }}">Back to list</a>
        {{ include('admin/reward/_form.html.twig') }}
    </div>

{% endblock %}
