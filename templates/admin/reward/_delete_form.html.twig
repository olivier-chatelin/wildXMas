<form method="post" action="{{ path('admin_reward_delete', {'id': reward.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reward.id) }}">
    <button class="btn btn-xMas m-1">Delete</button>
</form>
