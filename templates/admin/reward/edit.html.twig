{% extends 'base.html.twig' %}

{% block title %}Edit Reward{% endblock %}

{% block body %}
{% include 'components/_header.html.twig' %}
    <div class="container d-flex flex-column">
    <h1>Edit Reward</h1>
        <div class="d-flex justify-content-center align-items-center" >
            <div class="d-flex flex-column align-items-center">
                {{ include('admin/reward/_delete_form.html.twig') }}
                <a class="btn btn-gold  text-white" href="{{ path('admin_rewards') }}">Back to list</a>
            </div>
            <div class="align-self-center reward-alone m-1">
            {% include 'components/_reward.html.twig' %}
            </div>
        </div>
            {{ include('admin/reward/_form.html.twig', {'button_label': 'Update'}) }}
        <div class="d-flex">
        </div>

    </div>
{% endblock %}
