{% extends 'base.html.twig' %}
  {% block javascripts %}
      {{ parent() }}
      {{ encore_entry_script_tags('dynamic-input') }}
  {% endblock %}
{% block title %}{{ parent() }}-edit Reward{% endblock %}


{% block body %}
{% include 'components/_header.html.twig' %}
    <div class="container">
        <div class="d-flex justify-content-around align-items-center" >
            <div class="d-flex flex-column align-items-start">
                <h1>Update Reward</h1>
                {{ include('reward/_form.html.twig', {'button_label': 'Update'}) }}
                <a class="btn btn-gold  text-white m-1" href="{{ path('rewards') }}">Back to list</a>
                {{ include('reward/_delete_form.html.twig') }}
            </div>
            <div class="align-self-center reward-alone m-1">
                {{  include ('components/_reward.html.twig',{'size':'large'}) }}
            </div>
        </div>
    </div>
{% endblock %}
